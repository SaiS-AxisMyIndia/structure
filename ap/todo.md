>> create a rest-call in packages and add in module
It has to be work like a api call (http, fetch, axios)
in this also add all feature to call an api
SecureCall.get(route, .., .., ..)
add interceptor also
add self method which means you can call from inside service if found else call the api through url

- return page is not allowed for RestController only Packet create another one for Page create PageController for page in tester also read accordingly in PageController return type should be Page

// apc service:list
apc service:start auth
apc service:stop auth
apc service:restart auth
apc service:logs auth

// apc start -production -all

>> in entity add default to set the default value - if default not set then it is mandatory. Give any suggestion and possible required features

- remove microservices just flavours and port define

- I want to view the table data inside

>> add error besite props json button if any error found in the logs - app viewer

- instead of props use request with validator Page also same input ui in viewer app when click on props

>> change order of button in tester reponse (beautify, wrap, copy, curl)

- add app_viewer like tester. in this appviewer based on the Page return in controll show all in list on body section render the html and also add props json button on each page header below create body section

>> async and sync

- path encode the path param in tester and decode in gerogo request validator and return original value

- why I am getting token in every response. I just needt at login and refresh token only. In login I need token and refresh token also

- [x] device id in session and role(optional and it is list) also — AuthController::login()/register()/refresh()

- make sure default port for start is 7070

- avoid Page returns in tester

- in tester the fields we have to show as per request validator(required *, optional)

- cache the fills on the fields

- add token persistence save for all apis add at app bar right side

- by default collapse all controller bar in tester

- in prosql need #[ProEntity(<table name>)]
for each variable extention like #Primary(int/uuid/bigint etc), #Link(table.column), #Timestamp(?current, update = true?false)

- make sure while apc build find the gap of the table and create table when the .env(TABLE_WRITE=update/fixed/force). before that create one migration scripts to migrate any complexes need to fix first

- need curl, copy and wrap icons for the response in tester 
keep this icons at right side of status code row



=====
Documentation website
Interactive Tester / DevTools
Microservice CLI
Security
Automated tests + CI
A beautiful 5-minute quickstart